import React from 'react';
import { FlexBox, RequestButton, Input } from '@elements';

const SecondStep = () => {
  return (
    <FlexBox width='100%' height='100%' direction='column' css={{ gap: '25px' }}>
      <span css={{ width: '369px' }}>두번째 스텝</span>
    </FlexBox>
  );
};

export default SecondStep;
